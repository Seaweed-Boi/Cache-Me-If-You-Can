# Environment Configuration for Separate Server Deployment
# Copy this file to .env and update with your actual values

# ============================================
# RAG Application Server Configuration
# ============================================

# Local model configuration (optional)
LOCAL_MODEL_NAME=

# Embedding model configuration
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2

# Qdrant connection (when using same server)
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION=rag_documents

# Data path for PDFs
DATA_PATH=/app/data

# Enable Prometheus metrics
ENABLE_METRICS=true

# ============================================
# Locust Server Configuration
# ============================================

# RAG Application Server URL (update with your RAG server address)
# Examples:
#   RAG_APP_HOST=http://192.168.1.100:8000
#   RAG_APP_HOST=http://rag-server.example.com:8000
RAG_APP_HOST=http://localhost:8000

# ============================================
# Prometheus Server Configuration
# ============================================

# RAG Application Server for metrics scraping
# Update this in prometheus.yml instead of using environment variable
# Target format: <hostname>:<port>
# Example: 192.168.1.100:8000

# ============================================
# Network Configuration Notes
# ============================================

# When running on separate servers:
# 1. RAG App Server: Expose port 8000 (application + metrics)
# 2. Prometheus Server: Update prometheus.yml with RAG server IP
# 3. Locust Server: Set RAG_APP_HOST to RAG server URL
#
# Security: Use firewalls to restrict access between servers
# Production: Consider using HTTPS and authentication
